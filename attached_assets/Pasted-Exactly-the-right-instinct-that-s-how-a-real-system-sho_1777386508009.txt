Exactly the right instinct — that's how a real system should work. Here's what we'll build:

**The upgraded system:**

1. You tell the bot what you bought (ticker, shares, price)
2. It stores your portfolio
3. Every morning it analyzes ONLY your holdings and gives sell/hold signals
4. It also gives you just **1-2 buy recommendations** for new positions — only when there's a really strong opportunity

**You'll control it entirely from Telegram** with simple commands like:

```
/buy AAPL 10 189.50
/sell AAPL 5
/portfolio
```

And every morning you'll get a report like:

```
📊 Your Portfolio Report

AAPL — HOLD ✅
Up 4.2% since entry. Momentum strong. Target $210.

MSFT — SELL ⚠️
Down 9% from your entry $385. Stop loss hit. 
Recommendation: Exit position now.

VOO — HOLD ✅
Broad market stable. No action needed.

💡 New opportunity today:
NVDA — BUY signal. Strong earnings momentum.
```

---

**Type this in the Replit Agent chat:**

```
Rewrite main.py completely with this upgraded system:

1. A Telegram bot that accepts these commands:
   - /buy TICKER SHARES PRICE (e.g. /buy AAPL 10 189.50) - adds a position to portfolio
   - /sell TICKER (e.g. /sell AAPL) - removes a position from portfolio
   - /portfolio - shows current holdings with entry prices
   - /analyze - manually trigger analysis

2. Store the portfolio in a local JSON file called portfolio.json

3. Every day at 9AM UTC, automatically:
   - Fetch current prices for all stocks in the portfolio using yfinance
   - Calculate profit/loss % for each position since entry
   - Send data to Claude API to get HOLD or SELL signal for each position
   - Also fetch top 100 S&P 500 stocks and ask Claude to recommend maximum 2 new BUY opportunities
   - Send the full report to Telegram

4. For sell signals, Claude should consider:
   - Position is down more than 8% from entry (stop loss)
   - Position is up more than 25% (take profit)
   - Momentum has reversed
   - Better opportunities elsewhere

5. Use the ANTHROPIC_API_KEY and TELEGRAM_TOKEN from environment variables

6. Use Flask for a keep-alive web server

Keep the code clean and well commented so a beginner can understand it.
```

This will take the Agent a couple of minutes. Tell me when it's done and we'll test it together!